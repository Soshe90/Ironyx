<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Exact alarms, so a timer phase change is announced on the second: an
         inexact alarm was measured arriving 14 s late on a 20 s work phase,
         longer than a Tabata rest. USE_EXACT_ALARM is auto-granted on
         Android 13+ and Google Play restricts it to apps whose core function
         is an alarm or timer — the interval/rest timer is the justification
         for the Play Console declaration. SCHEDULE_EXACT_ALARM covers
         Android 12/12L only (capped, so it is never requested where
         USE_EXACT_ALARM applies). If Play rejects the declaration, delete
         both lines: NotificationService asks the OS at schedule time and
         falls back to inexact alarms, so nothing else needs to change. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32"/>

    <!-- allowBackup defaults to true, which would hand Android's Auto Backup
         (and, on 12+, the same opt-out also covers device-to-device
         migration) a copy of this app's SharedPreferences — including the
         Supabase session's refresh token, stored in plaintext by
         supabase_flutter, not the Keystore — plus the SQLite database
         itself, containing training history and personal details. Google
         Drive is not a place either of those should end up. The app already
         has its own export file and cloud-restore feature for this. -->
    <application
        android:label="Ironyx"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Supabase auth callback. Email confirmation and password-reset
                 links redirect here so they reopen the app instead of the
                 project's default Site URL (http://localhost:3000), which no
                 phone can load. The scheme must match DeepLinks.authCallback
                 and the Supabase dashboard's Redirect URLs allow-list.
                 BROWSABLE is what lets a browser or mail client follow it;
                 DEFAULT is what lets it be resolved by an implicit intent. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="com.soshe90.ironyx"
                    android:host="login-callback"/>
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications does NOT declare these itself; its own
             manifest carries permissions only. Without the first, a scheduled
             timer notification's alarm fires, Android delivers the broadcast
             to a component that does not exist, and drops it silently — the
             notification never appears (this is what shipped before). The
             second re-arms pending notifications after a reboot or an app
             update, which is what RECEIVE_BOOT_COMPLETED above is for. Both
             must stay non-exported: only the plugin's own alarms and the
             system's boot broadcast should reach them. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
